# Server port (default: 3001)
PORT=3001

# Allowed CORS origin — must match the URL the browser loads the client from
# (default: http://localhost:5173 for Vite dev server)
CLIENT_ORIGIN=http://localhost:5173

# Node environment
NODE_ENV=development
